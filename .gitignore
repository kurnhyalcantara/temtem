# Binaries
/bin/
/dist/
*.exe

# Test & coverage
coverage.out
coverage.html

# Local config overrides (never commit real config — use TEMTEM_ env vars in production)
config/config.yaml
config/config.local.yaml

# Compose env files (hold platform secrets — TEMTEM_POSTGRES__PASSWORD, TEMTEM_JWT__SECRET)
.env
deployments/.env

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
